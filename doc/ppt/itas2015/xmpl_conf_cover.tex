\documentclass[crop,english]{wiasconf}
    % language:  "ngerman" (default), "english", "german"
    % "crop":    print crop marks for professional printing
    % "twoside": layout with front (right side) and back (left side) page
    % "oneside": layout with front page only
\usepackage[utf8]{inputenc}
    % input encoding, maybe you want "latin1" or "ansinew"

% The content of this file can also be put directly here, but some data
% are used by the conference poster, too, so we use a common file.
\input{xmpl_conf_data}

% space between paper margin and printed stuff
%\setlength{\pagemargin}{10mm}

\begin{document}
\printcover
\end{document}
